apiVersion: crunchydata.com/v1
kind: Pgcluster
metadata:
  annotations:
    current-primary: {{ include "zoru.fullname" . }}
  labels:
    crunchy-pgha-scope: {{ include "zoru.fullname" . }}
    name: {{ include "zoru.fullname" . }}
    pg-cluster: {{ include "zoru.fullname" . }}
    pgo-version: 4.6.0
    pgouser: admin
    {{- include "zoru.labels" . | nindent 4 }}
  name: {{ include "zoru.fullname" . }}
  namespace: pgo
spec:
  BackrestStorage:
    accessmode: ReadWriteOnce
    size: 100G
    storageclass: "standard"
    storagetype: dynamic
  PrimaryStorage:
    accessmode: ReadWriteOnce
    size: 100G
    storageclass: "standard"
    storagetype: dynamic
  ReplicaStorage:
    accessmode: ReadWriteOnce
    size: 100G
    storageclass: "standard"
    storagetype: dynamic
  annotations: {}
  ccpimage: crunchy-postgres-ha
  ccpimageprefix: registry.developers.crunchydata.com/crunchydata
  ccpimagetag: centos8-13.1-4.6.0
  clustername: {{ include "zoru.fullname" . }}
  database: kon
  exporterport: "9187"
  limits: {}
  name: {{ include "zoru.fullname" . }}
  namespace: pgo
  pgDataSource:
    restoreFrom: {{ .Values.restoreFrom }}
    restoreOpts: ""
  pgbadgerport: "10000"
  pgoimageprefix: registry.developers.crunchydata.com/crunchydata
  podAntiAffinity:
    default: preferred
    pgBackRest: preferred
    pgBouncer: preferred
  port: "5432"
  tolerations: []
  user: zoru
  userlabels:
    pgo-version: 4.6.0
